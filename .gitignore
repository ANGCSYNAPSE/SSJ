# Each app keeps its own ignore rules in frontend/ and backend/.
# This file covers only what sits at the repo root.

# dependencies
node_modules

# local env files
.env
.env*.local

# editor / OS
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
