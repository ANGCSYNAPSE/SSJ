import type { Metadata } from "next";

export const metadata: Metadata = { title: "Dancer Registration - Shyam Jagat" };

export default function DancerRegistrationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
