import type { Metadata } from "next";

export const metadata: Metadata = { title: "Temple Registration - Shyam Jagat" };

export default function TempleRegistrationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
