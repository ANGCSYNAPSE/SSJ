import type { Metadata } from "next";

export const metadata: Metadata = { title: "Dharamshala Registration - Shyam Jagat" };

export default function DharamshalaRegistrationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
