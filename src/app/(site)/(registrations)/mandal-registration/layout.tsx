import type { Metadata } from "next";

export const metadata: Metadata = { title: "Mandal & Organisation Registration - Shyam Jagat" };

export default function MandalRegistrationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
